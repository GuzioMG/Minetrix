// Install plugins
plugins {
    id 'fabric-loom' version "${loom_version}"
    id 'maven-publish'
}

// Now that the plugins are installed, do the actual build-script
apply from: './build_postplugin.gradle'